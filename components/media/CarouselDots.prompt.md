Dot indicators for a carousel. Active dot is a highlight-blue capsule; inactive dots are small.

```jsx
<CarouselDots count={6} active={i} onSelect={setI} />
```

- The only rounded shapes besides pills/toggles. Center under hero carousels.
