Labelled dropdown. Same flush dark fill as `Input`, chevron on the right, grey placeholder.

```jsx
<Select label="Billing Country" placeholder="Select Billing Country..."
  options={["United States","Canada","United Kingdom"]} value={c} onChange={setC} />
```

- Pairs with a help "?" badge to its right on the payment form.
