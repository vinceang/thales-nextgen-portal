Flight progress strip (origin → plane → destination + duration) shown atop the side drawer.

```jsx
<FlightTracker origin="LAX" destination="MCO" duration="3h 28m" progress={0.35} />
```

- Codes and plane glyph are bright-blue on the black drawer. `progress` is 0–1.
